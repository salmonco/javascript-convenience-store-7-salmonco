# javascript-convenience-store-precourse

## 기능 요구 사항

구매자의 할인 혜택과 재고 상황을 고려하여 최종 결제 금액을 계산하고 안내하는 결제 시스템을 구현한다.

- 사용자가 입력한 상품의 가격과 수량을 기반으로 최종 결제 금액을 계산한다.
  - 총구매액은 상품별 가격과 수량을 곱하여 계산하며, 프로모션 및 멤버십 할인 정책을 반영하여 최종 결제 금액을 산출한다.
- 구매 내역과 산출한 금액 정보를 영수증으로 출력한다.
- 영수증 출력 후 추가 구매를 진행할지 또는 종료할지를 선택할 수 있다.
- 사용자가 잘못된 값을 입력할 경우 "[ERROR]"로 시작하는 메시지와 함께 Error를 발생시키고 해당 메시지를 출력한 다음 해당 지점부터 다시 입력을 받는다.

**재고 관리**

- 각 상품의 재고 수량을 고려하여 결제 가능 여부를 확인한다.
- 고객이 상품을 구매할 때마다, 결제된 수량만큼 해당 상품의 재고에서 차감하여 수량을 관리한다.
- 재고를 차감함으로써 시스템은 최신 재고 상태를 유지하며, 다음 고객이 구매할 때 정확한 재고 정보를 제공한다.

**프로모션 할인**

- 오늘 날짜가 프로모션 기간 내에 포함된 경우에만 할인을 적용한다.
- 프로모션은 N개 구매 시 1개 무료 증정(Buy N Get 1 Free)의 형태로 진행된다.
- 1+1 또는 2+1 프로모션이 각각 지정된 상품에 적용되며, 동일 상품에 여러 프로모션이 적용되지 않는다.
- 프로모션 혜택은 프로모션 재고 내에서만 적용할 수 있다.
- 프로모션 기간 중이라면 프로모션 재고를 우선적으로 차감하며, 프로모션 재고가 부족할 경우에는 일반 재고를 사용한다.
- 프로모션 적용이 가능한 상품에 대해 고객이 해당 수량보다 적게 가져온 경우, 필요한 수량을 추가로 가져오면 혜택을 받을 수 있음을 안내한다.
- 프로모션 재고가 부족하여 일부 수량을 프로모션 혜택 없이 결제해야 하는 경우, 일부 수량에 대해 정가로 결제하게 됨을 안내한다.

**멤버십 할인**

- 멤버십 회원은 프로모션 미적용 금액의 30%를 할인받는다.
- 프로모션 적용 후 남은 금액에 대해 멤버십 할인을 적용한다.
- 멤버십 할인의 최대 한도는 8,000원이다.

**영수증 출력**

- 영수증은 고객의 구매 내역과 할인을 요약하여 출력한다.
- 영수증 항목은 아래와 같다.
  - 구매 상품 내역: 구매한 상품명, 수량, 가격
  - 증정 상품 내역: 프로모션에 따라 무료로 제공된 증정 상품의 목록
  - 금액 정보
    - 총구매액: 구매한 상품의 총 수량과 총 금액
    - 행사할인: 프로모션에 의해 할인된 금액
    - 멤버십할인: 멤버십에 의해 추가로 할인된 금액
    - 내실돈: 최종 결제 금액
- 영수증의 구성 요소를 보기 좋게 정렬하여 고객이 쉽게 금액과 수량을 확인할 수 있게 한다.

## 구현할 기능 목록

- 상품 목록과 행사 목록을 파일 입출력을 통해 불러온다.
- 환영 인사와 함께 상품명, 가격, 프로모션 이름, 재고를 안내한다. 만약 재고가 0개라면 재고 없음을 출력한다.
  - 프로모션 재고는 있지만 일반 재고가 없을 경우, 일반 재고에 대해 재고 없음을 출력한다.
- 구매할 상품명과 수량을 입력받는다.
- 개별 상품을 쉼표(,)로 파싱한다.
- 상품명과 수량을 하이픈(-)으로 파싱한다.
- 프로모션 적용이 가능한 상품인지 확인한다.
- 프로모션 적용이 가능한 상품에 대해 고객이 해당 수량보다 적게 가져왔는지 확인한다.
  - 프로모션 buy 수량보다 적게 가져왔을 경우 더 가져올 건지 묻지는 않는다.
  - 프로모션 buy 수량만큼 가져왔음에도 get 수량보다 적게 가져왔을 경우 더 가져올 건지 묻는다.
  - 수량보다 적게 가져왔을 경우, 혜택에 대한 안내 메시지를 출력한다.
  - 수량보다 적게 가져왔을 경우, 그 수량만큼 추가 여부를 입력받는다.
  - Y: 증정 받을 수 있는 상품을 추가한다.
  - N: 증정 받을 수 있는 상품을 추가하지 않는다.
- 프로모션 재고가 부족하여 일부 수량을 프로모션 혜택 없이 결제해야 하는지 확인한다.
  - 프로모션 혜택 없이 결제해야 하는 경우, 일부 수량에 대해 정가로 결제할지 여부에 대한 안내 메시지를 출력한다.
  - 프로모션 혜택 없이 결제해야 하는 경우, 일부 수량에 대해 정가로 결제할지 여부를 입력받는다.
  - Y: 일부 수량에 대해 정가로 결제한다.
  - N: 정가로 결제해야하는 수량만큼 제외한 후 결제를 진행한다.
- 멤버십 할인 적용 여부를 확인하기 위해 안내 문구를 출력한다.
- 멤버십 할인 적용 여부를 입력 받는다.
  - Y: 멤버십 할인을 적용한다.
    - 프로모션 미적용 금액의 30%를 계산한다.
    - 멤버십 할인의 최대 한도는 8,000원을 초과하는지 확인한다.
  - N: 멤버십 할인을 적용하지 않는다.
- 추가 구매 여부를 입력 받는다.
  - Y: 재고가 업데이트된 상품 목록을 확인 후 추가로 구매를 진행한다.
  - N: 구매를 종료한다.
- 구매 상품 내역, 증정 상품 내역, 금액 정보를 영수증 형식으로 출력한다.
  - 구매 상품 내역: 구매한 상품명, 수량, 가격을 구한다.
  - 증정 상품 내역: 프로모션에 따라 무료로 제공된 증정 상품의 목록을 구한다.
  - 금액 정보:
    - 총구매액: 상품별 가격과 수량을 곱하여 총구매액을 계산한다.
    - 행사할인: 프로모션에 의해 할인된 금액을 구한다.
    - 멤버십할인: 멤버십에 의해 추가로 할인된 금액을 구한다.
    - 내실돈: 최종 결제 금액을 계산한다.
- 각 상품의 재고 수량을 고려하여 결제 가능 여부를 확인한다.
- 오늘 날짜가 프로모션 기간 내에 포함되었는지 확인한다.
- 구매할 상품의 수량이 프로모션 재고 내에 포함되는지 확인한다.
- 프로모션 이름에 따라 몇 개를 사면 몇 개가 증정되는지 확인한다.
- 프로모션 기간 중이라면 프로모션 재고를 우선적으로 차감하며, 프로모션 재고가 부족할 경우에는 일반 재고를 사용한다.
- 사용자가 잘못된 값을 입력할 경우 "[ERROR]"로 시작하는 메시지와 함께 Error를 발생시키고 해당 메시지를 출력한 다음 해당 지점부터 다시 입력을 받는다.

## 예외 상황

- 구매할 상품과 수량 형식이 올바르지 않은 경우: [ERROR] 올바르지 않은 형식으로 입력했습니다. 다시 입력해 주세요.
- 존재하지 않는 상품을 입력한 경우: [ERROR] 존재하지 않는 상품입니다. 다시 입력해 주세요.
- 구매 수량이 재고 수량을 초과한 경우: [ERROR] 재고 수량을 초과하여 구매할 수 없습니다. 다시 입력해 주세요.
- 기타 잘못된 입력의 경우: [ERROR] 잘못된 입력입니다. 다시 입력해 주세요.

## 체크 리스트
### 개발 과정
- 일단 요구 사항대로 돌아가게 하자
    - 기능 목록을 작성했는지
    - 기능 단위로 커밋하고 있는지
    - 예외 상황을 작성했는지
- 그 다음 리팩토링하자
    - 함수가 한 가지 역할만 하는지 (단일책임)
    - 비즈니스 로직과 UI 로직이 분리되었는지
        - 한 클래스 내에서 같이 처리X
        - InputView, OutputView
    - indent depth가 2 이내인지
    - 함수 길이가 10라인 이내인지
    - 클래스 내 필드(인스턴스 변수)의 수가 적절한지
        - 필드의 수가 많아지면 객체의 복잡도가 증가하고 관리가 어려워진다.
    - 3항 연산자를 쓰지 않았는지
    - early return 하였는지
    - 개행 등 코드 포맷팅을 지켰는지
    - 네이밍이 적절한지
    - 전반적인 코드의 추상화 수준이 적절한지
    - 상수화가 잘 되었는지
- 테스트 코드 작성하자
    - 작은 단위로 테스트가 작성되었는지 (함수, 기능 단위)
        - 테스트 코드 역시 코드의 일환이므로, 리팩터링을 통해 지속적으로 개선해 나가는 것이 중요하다.
        - 테스트를 위한 코드는 구현 코드에서 분리되어야 한다.
        - 테스트 코드를 작성하다 보면 테스트를 더 쉽게 하기 위해 접근 제어자를 변경하거나, 테스트에서만 사용되는 메서드를 구현 코드에 추가하는 경우가 있다. 그러나 이렇게 하면 구현 코드가 테스트에 종속되며, 캡슐화가 깨지고 코드의 일관성이 저해된다.

### 피어리뷰
- 불리언값 반환하는 함수를 무지성 is~~로 네이밍 했었는데, has나 can 등 적절한 네이밍 붙이는 게 좋겠다.
- import문 깔끔해지게 배럴파일 써보는 거 어떻겠냐
- cat, str, arr 등 어차피 짧은 단어라 풀어써도 괜찮을 것 같다.
- 숫자타입 변환할 때 + 단항연산자보다 Number를 쓰는 게 의미전달이 명확할 것 같다.
- 테스트 코드 중복 줄일 때 test.each() 쓰는 게 좋겠다.
- 클래스 내부에서만 사용되는 메서드는 private하게 관리하는 게 좋겠다.
- 클래스 내에서 static 키워드 안 붙은 필드는 모두 인스턴스 필드로 추가된다.
- 클래스 내 필드 접근제한자 기준에 대한 고민
    - private : 인스턴스 변수 사용, 클래스 내부에서만 사용할 때
    - public : 인스턴스 변수 사용, 클래스 외부에서도 사용할 때
    - static : 인스턴스 변수를 사용하지 않음, 클래스 외부에서도 사용할 때
- 클래스 내 private 필드 테스트하기 어려우면 jest의 spy 기능을 사용해보자

## 회고

### 테스트를 작성하는 이유는 뭘까?
- 빠른 피드백, 안정감?
- 구현해야 할 기능만 우선적으로 개발하게 도와서?
    - 오버 엔지니어링 방지
- 코드 변경 시 다른 코드에 영향이 가는 걸 알고 방지하기 위해?

개인적으로 나는 테스트를 작성하는 이유 중 가장 큰 게 구현해야 할 기능만 우선적으로 개발하게 도와서라고 생각했다. 즉 오버 엔지니어링을 방지하고자 하는 것이다. 부가적인 효과로 코드 변경시 다른 코드에 영향이 가는 걸 알고 방지하는 것과, 빠른 피드백을 통한 안정감이 있을 수 있다고 생각했다. 그렇기에 우선 구현할 기능 목록을 작성하는 것만으로 TDD와 비슷한 효과를 볼 수 있다고 생각했다. 주어진 기능 목록만 우선적으로 구현하게 되기 때문이다. TDD는 기능 목록을 실제 코드에서 확인할 수 있기 때문에 훨씬 효과적이라고 생각한다. 하지만 지금 나는 테스트 코드 작성에 익숙지 않은 상태다. 테스트 코드 작성 시간의 오버헤드가 커질 거라고 생각해 시간이 없는 상황에서 TDD를 적극 적용하지 못했다. 향후 테스트 코드 작성법을 습득할 것이다. 그리고 TDD 방법론에 의해 코딩하는 것에 익숙해지려 한다.

### 4주차 과제 소감

요구사항이 많아짐에 따라 코드 또한 복잡해져서 어렵게 느껴졌다. 처음에는 리팩토링 신경 쓰지 말고 한 App.js 파일 내에서 요구사항부터 돌아가게끔 만들 생각이었다. 하지만 요구사항이 많았기 때문에 오히려 한 파일 내에서 모든 코드를 작성했던 것이 더욱 복잡하게 다가왔다. 이에 다른 파일로 점차 분리하게 되었다. 그 과정에서 같은 데이터를 처리하는 함수들을 묶어 클래스로 만들고자 했다. 하지만 어떻게 역할을 나누어 분리해야 효과적으로 돌아갈지 알기 어려웠다. 지난 로또 과제에서 Lotto 클래스를 만든 경험을 바탕으로 Product 클래스를 모델로 사용하고자 했다. 클래스 기반 프로그래밍에 감이 아직 부족한 상태이기에 다른 사람 코드를 보며 배울 것이다.

한편, 지난 주차에서 얻은 피드백을 바탕으로 체크 리스트를 만들었다. 이를 리팩토링 과정에서 체크하면서 코드 품질을 지키고자 했다. 하지만 요구사항을 만족하기에 급급해 리팩토링 시간을 충분히 가지지 못했다. 그 점이 아쉬웠다.

### 프리코스 회고

중간 회고에서 코드리뷰 3번 하는 목표를 세웠다. 그리고 그 목표를 달성했다. 코드리뷰를 목표로 잡은 이유는 프리코스를 통해 코드리뷰 하며 배우는 것이 많다고 느꼈기 때문이다. 확실히 다른 사람 코드를 보고 배우는 게 많았고, 내 코드를 피드백받으며 고민하는 시간이 늘었다. 개발자 커뮤니티, 코드리뷰 문화를 지속적으로 경험함으로써 함께 일하는 더 나은 개발자로 성장할 수 있음을 깨달았다.

매 주차 과제는 대략 7일의 시간이 주어지는데 학업과 병행하는 등 개인적인 일로 인해 매번 과제 수행에 2일 밖에 시간을 투자하지 못했다. 그래서 아쉬웠다. 시간을 투자하는 만큼 더 많이 배울 수 있는 과정이라 생각했기 때문이다. 하지만 과정 이후에도 다른 사람의 코드를 보며 배울 수 있는 기회는 많다고 생각한다. 빠르게 배워서 성장하고자 한다.

결론적으로 프리코스 과정을 통해 이전보다 코드를 보는 관점이 넓어졌다고 생각한다. 네이밍 등 가독성을 고려하게 되었고 더 나은 방법은 없을까를 고민하게 되었다. 성능과 가독성의 트레이드오프에 대해 고민하며 초기에는 가독성을 챙기되 이후 규모가 커지면 성능 또한 고려할 생각을 하게 되었다. 비즈니스 로직과 UI 로직을 분리하는 게 유지보수에 좋겠다는 생각을 하게 되었고, 함수를 짤 때 한 가지 일만 하는지 생각해보게 되었다. 이전에는 매번 콘솔을 찍는 식으로 디버깅했었는데, vscode의 디버깅 기능에 익숙해질 수 있었다.

### TODO

- 테스트 코드 작성법 습득
- 클래스 기반 프로그래밍 방법 학습
- 다른 사람 코드리뷰 10회
